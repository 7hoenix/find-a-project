<h1>Waza Projects</h1>
<h3>Add a Project</h3>
<%= form_for :project, url: projects_path do |f| %>
  <%= f.text_area :description %>
  <%= f.submit "Create Project" %>
<% end %>

There are currently <%= @projects.count %> active projects.

<ul>
  <% @projects.each do |project| %>
    <li>
      <%= project.description %>
    </li>
  <% end %>
</ul>
